import UniversalLoading from "@/components/ui/universal-loading";

export default function Loading() {
  return <UniversalLoading message="Loading property details" detail="Preparing gallery, specs, and location..." />;
}
