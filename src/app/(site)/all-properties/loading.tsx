import UniversalLoading from "@/components/ui/universal-loading";

export default function Loading() {
  return <UniversalLoading message="Loading properties" detail="Preparing listings and filters..." />;
}
