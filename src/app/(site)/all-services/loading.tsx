import UniversalLoading from "@/components/ui/universal-loading";

export default function Loading() {
  return <UniversalLoading message="Loading services" detail="Preparing service catalog..." />;
}
