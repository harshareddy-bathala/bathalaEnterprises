import AdminRouteLoading from "@/components/admin/admin-route-loading";

export default function Loading() {
  return (
    <AdminRouteLoading
      title="Loading messages"
      description="Building inbox filters, statuses, and conversation details..."
    />
  );
}
