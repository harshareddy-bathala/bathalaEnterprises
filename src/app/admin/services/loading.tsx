import AdminRouteLoading from "@/components/admin/admin-route-loading";

export default function Loading() {
  return (
    <AdminRouteLoading
      title="Loading services"
      description="Preparing service cards, ordering, and actions..."
    />
  );
}
