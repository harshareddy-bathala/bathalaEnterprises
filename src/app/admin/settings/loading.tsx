import AdminRouteLoading from "@/components/admin/admin-route-loading";

export default function Loading() {
  return (
    <AdminRouteLoading
      title="Loading settings"
      description="Preparing profile, security, and notification controls..."
    />
  );
}
